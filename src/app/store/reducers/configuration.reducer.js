import * as actionType from "./../../units/actions.type";
import * as initState from "../state/configuration.init";

const configuration = (state = initState.configuration, action) => {
	switch (action.type) {
		default:
			return state;
	}
};

export default configuration;